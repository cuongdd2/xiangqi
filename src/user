CREATE TABLE user if NO EXISTS {
  id INT(5) NOT NULL DEFAULT 0 AUTO_INCREMENT,
  name CHAR(10) NO NULL DEFAULT '',
  winRate DOUBLE(4) NO NULL DEFAULT 0,
  eleResult INT(8) NO NULL DEFAULT 0,
  PRIMARY KEY id
};